# RL 보행 학습 — 12시간 자율 작업 로그

**작업자**: Claude (자율 모드) · **승인**: 승윤님 · **기간**: 2026-07-19 03:00 ~ 15:00 KST
**목표**: 12DOF 하체 로봇의 보행을 Isaac Lab 병렬 PPO로 학습 — **실물 제약(kd≤5, 50Hz, 슬루 4°/tick)을 학습 루프에 내장**하여 sim2real 이식성 확보
**증거자료**: `log_picture/` (인덱스: `자료목록.md`) · **최종보고서**: `docs/RL보행_최종보고서.md` (작업 종료 시 작성)

---

## 사전 확정 사항 (02:40~03:00)

### 환경 실측
- GPU: RTX 5080 16GB (Blackwell sm_120, driver 580.159.04) / CPU 24코어 / RAM 62GB / 디스크 여유 270GB
- OS: Ubuntu 22.04.5, 자동절전 꺼짐, cmake·gcc 기설치 (sudo 불필요)
- Isaac Sim 5.1.0 바이너리: `/home/ryu/isaacsim/isaac-sim-5.1.0` (python 3.11.13)

### 방법 결정 (대안 비교 후)
- **채택**: Isaac Lab v2.3.2 + rsl_rl PPO, velocity-tracking 태스크
- 기각: MuJoCo Playground(관절 부호 12개 재검증 비용 + 엔진 불일치), legged_gym(지원 종료), Isaac Lab v3.0 베타(Isaac Sim 6.0 전용 — 5.1과 비호환)
- 원래 플랜에서 수정한 3가지:
  1. **kd≤5를 학습 환경에 내장** (kd25로 학습하면 FSM과 동일한 전이 실패 반복 — 게인 정합 실험의 교훈)
  2. FSM 교사(KL 모방)는 보험으로 강등 — 표준 velocity-tracking 먼저, 서기 함정 시에만 투입
  3. 씬이 아니라 로봇 자산(URDF + config JSON)을 포팅

### 리서치 3방향 (병렬 에이전트, 원본: log_picture/00_0242)
1. **설치**: v2.3.2 확정 (Isaac Sim 5.1 공식 지원 최종판). torch 2.7.0+cu128 자동 설치 = sm_120 포함. Blackwell 유일 이슈는 TiledCamera 행업(우리 무관). 항상 `./isaaclab.sh -p`로 실행할 것
2. **레시피**: Berkeley Humanoid Lite **12DOF biped** 태스크가 최근접 선례. 보상 21항 증류(가중치 출발점 포함), 저kd 전략 10종, 서기 함정 회피 9종, DR 범위 15항
3. **로봇 스펙**: URDF 관절 전부 continuous(리밋 없음) → config JSON에서 주입 필수. hip_a 양다리 동시 내전 ±9.4° 충돌 → 자기충돌 ON 필수. 총질량 9.937kg, 발판 0.18×0.078m 전후대칭(발목이 중심에서 9.7mm 뒤). 실물 게인: hip_f/knee kp30 kd2, …, ankle_r kp15 kd1 (kd 상한 5)

---

## 작업 타임라인

### 03:0x — Phase 0: 설치
- Isaac Lab v2.3.2 체크아웃 (`37ddf62687`), `_isaac_sim` 심링크, python 3.11.13 확인
- 밀려있던 보행 UI 수정분 커밋 (`131c5ec`)
- **함정 1**: `isaaclab.sh`가 파이썬 에러를 삼키고 exit 0을 반환 → "성공"으로 보인 설치·검증이 실제론 실패였음. 이후 모든 검증은 exit code 대신 산출물/로그 마커로 판정
- **함정 2**: `--install`이 부속 5개 패키지만 설치하고 **코어 `isaaclab`을 누락** — 원인: 빌드 격리 환경의 최신 setuptools에서 `pkg_resources` 제거됨. 해결: `pip install -e source/isaaclab --no-build-isolation` → isaaclab 0.54.2 OK
- 검증 통과: torch 2.7.0+cu128, **sm_120(RTX 5080) 인식 True** (증거: log_picture/01)

### 03:0x~03:10 — Phase 1: 자산 포팅
- **모터 스펙 정정 반영** (승윤님 제보): AK70-10 ×10 + **발목롤만 AK45-36 ×2**. 웹 조사로 공식 스펙 확보 — AK45-36은 피크 24Nm으로 동급이지만 **속도한계 ±6rad/s(AK70-10의 1/8)**, 반사관성 0.0236kg·m²(13배). 액추에이터 2그룹으로 분리 (`biped12_cfg.py`)
- URDF에 **base 링크 주입** (Rz90°·Rx90° 고정조인트): SolidWorks 산출 pelvis 프레임(Y-up, Z-전방)을 Isaac Lab 표준(Z-up, X-전방)으로. `--merge-joints`로 pelvis가 base에 병합되어 루트 바디 = Z-up "base"
- 변환 검증 (증거: log_picture/02): 총질량 9.9379kg 일치, 관절 12개 실측 리밋 주입, 축 매핑 정상
- 학습 환경 작성: 정책 50Hz(=실물 노드 틱), 슬루 4°/tick 커스텀 액션, kp150/kd5(±20% DR), 액터 관측은 실물 측정 가능 신호만(IMU+엔코더), 크리틱 특권 관측, 보상 21항(G1/BHL/humanoid-gym 증류), DR 소형 스케일

### 03:10~03:35 — 새벽의 디버깅: "왜 서 있질 못하니"
- 스폰 검증(증거: log_picture/03): 기하·질량·관절순서·관측차원 전부 정상인데 **무액션 개루프 2초 내 전도**
- A/B 게인 진단(log_picture/04): 구 검증 게인 kp250/kd25로도 전도 → 자산 버그 의심 국면
- **가설 1 (관절 부호 뒤집힘)**: 무중력+루트동결 12관절 프로브(log_picture/05)로 **전부 정상 확인 — 기각**. hip_f 굴곡→무릎 전방 +8.2cm, knee 굴곡→발목 후방 −9.1cm, ankle_f 배굴→발 전방 +2.5cm, ankle_r 좌우 미러 패턴까지 ANAT_SIGN과 완전 일치
- **가설 2 (왼발 충돌체 소실)**: 접촉 전수조사(log_picture/06)로 기각 — 양발 46-56N, 합≈체중 98N 정확. "왼발 0N"은 관절체 인덱스를 센서 인덱스에 꽂은 내 진단 스크립트 버그였음
- **진짜 원인**: 자기충돌 OFF에서 pgx −0.16(뒤로 9°)의 **안정 평형** 발견 + 발목 편차×kp=인가토크 정확 일치(0.045rad×250=11.3Nm) → **자산은 물리적으로 정상. 개루프 후방 기울음은 이 로봇의 실제 특성** (구 시뮬도 트림 전 3.46° 후방 기울 + 직립 유지에 상시 피드백 필요했음 — 같은 성질). 자기충돌 ON은 이 한계 평형을 살짝 넘어뜨리는 정도의 미세 외란
- **결정**: 자산 승인. 자기충돌 ON 유지(실물 hip_a ±9.4° 충돌 보호). 개루프 직립은 요구조건이 아님 — 밸런스 루프는 RL 정책이 담당 (G1 등 모든 보행 RL 환경이 동일)

### 03:35~04:30 — Phase 2: 학습 개시
- 스모크 학습 (4096 env × 100 iter): **208,000 스텝/초** (RTX 5080), 보상 −0.47→+21, 생존 886/1000, NaN 0 — 풀 학습 1회 ≈ 30분으로 판명 (예상의 1/5)
- 풀 학습 런1 (3000 iter) 발사. iter 725 시점: 보상 41.7, 생존 989/1000, **속도추종 90%**
- 병렬로 환경 코드 적대적 리뷰 3렌즈 실행. 슬루 액션 렌즈 결과: **결함 0** — 50Hz 호출 주기·실물 노드 슬루와의 정합·리셋 타이밍·ONNX 경로 전부 검증 통과
- 커밋 `a3ef882` (Phase 0-1 전체)
- 대기 시간에 선행 제작: 학습곡선 플롯 도구, 정량평가 스크립트(eval_policy.py — AK45-36 속도한계 준수 검사 포함), walk_scene 재생 어댑터(policy_adapter.py — 프레임 변환 Rz90·Rx90 + 이름 매핑 + 50Hz 클록/슬루), 원커맨드 RL 데모(scripts/rl_walk_demo.py), **낮은 계단 험지 환경(rough_env_cfg.py — 단높이 2~6cm, terrain curriculum)**, 이식 가이드 문서(docs/RL보행_이식가이드.md)

### 04:30~04:55 — 리뷰 반영 + 런1 결과
- 리뷰 3렌즈 완료 (에이전트 3개, 32만 토큰): findings 6건 — major 3 / minor 3, verified_ok 28건
  - [해소] "SC ON 전도 미해결" → **0.001N 정밀 전수조사(log_picture/07): 유령 자기접촉력 전무** (기립+양다리 내전 재현까지). SC ON/OFF 차이는 솔버 수치 효과 = 물리적 무해 판정, SC ON 유지
  - [수정] 진단 스크립트 2종의 센서 인덱싱 버그 (관절체 바디순서 BFS ≠ 접촉센서 DFS — "왼발 0N" 미스터리의 정체), 게이트에 양발 접촉력 검사 추가
  - [수정] 마찰 DR make_consistent, armature 주석 정합
  - 슬루 액션 렌즈: **결함 0** (50Hz 주기·실물 노드 정합·리셋 타이밍·ONNX 경로 전부 검증)
- **런1 (3000 iter, 29분, 20만 스텝/s) 결과 — 첫 풀 학습에 보행 성공:**
  - 정량평가(64env×30s, cmd 0.5m/s): **낙상 0**, 전진 13.3m/15m, 추종오차 0.061m/s
  - **직립 평균 2.25° (FSM 피크 15-18° 대비)**, 케이던스 **2.0걸음/s = 사람 보행 영역(121spm)**
  - 단일지지 92.7% (동적보행), 토크 평균 4.5Nm(여유 82%), **발목롤 4.03rad/s < AK45-36 한계 5.0**
  - 영상(log_picture/09): 9대 전원 직립 보행, 학습곡선(08)
  - 정책 익스포트 완료: exported/policy.pt(.onnx) — 이식 준비물
- 커밋 `7ecebe1`

### 04:55~ — 2단계 학습 (강건화·다재화)
- stage2 cfg: 명령 확장(전진 0.2-0.8, 측방 ±0.25, 요 ±1.0, 서기 10%), 외란 ±0.5m/s@4-8s, 마찰 0.2-1.25, 부드러움 페널티 상향, 에어타임 0.5로 강등 — 런1 리줌
- 함정 3: `--resume True`는 hydra 오버라이드 파싱 오류 (store_true 플래그) → `--resume`으로 수정 재발사

(이하 진행하며 계속 기록)
